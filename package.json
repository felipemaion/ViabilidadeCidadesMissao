{
  "name": "cidades-missao-dashboard",
  "private": true,
  "scripts": {
    "build-data": "python3 scripts/build_dashboard_data.py",
    "build-programa": "python3 scripts/build_programa_reforma.py",
    "build-site": "npm run build-data && npm run build-programa && npm run preparar-publicacao",
    "preparar-publicacao": "python3 scripts/prepare_static_publish.py",
    "serve": "python3 scripts/serve_dashboard.py",
    "test": "python3 -m unittest discover -s tests -v && node --test tests/test_api_tributos.mjs"
  }
}
